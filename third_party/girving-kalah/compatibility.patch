diff --git a/Makefile b/Makefile
--- a/Makefile
+++ b/Makefile
@@ -4,6 +4,6 @@
 CC = gcc
 CILK = cilk
-CFLAGS = -O3 -funroll-loops -Winline -Wall -fnested-functions
+CFLAGS = -O3 -funroll-loops -Winline -Wall -std=gnu11 -fgnu89-inline
 CILKFLAGS = $(CFLAGS) #-cilk-profile -cilk-critical-path
 
 ELIDE = -DNOCILK -x c
diff --git a/mix.h b/mix.h
--- a/mix.h
+++ b/mix.h
@@ -49,3 +49,2 @@ static inline long packpits(char *a) {
   #elif PITS == 6
-  #define PIT_COMBINE *++a | *++a | *++a | *++a | *++a
   #endif
@@ -53,3 +52,9 @@ static inline long packpits(char *a) {
+#if PITS == 6
+  if ((a[0] | a[1] | a[2] | a[3] | a[4] | a[5]) & ~31)
+    return HASHOVERFLOW;
+  return (long)a[0] | ((long)a[1] << 5) | ((long)a[2] << 10)
+      | ((long)a[3] << 15) | ((long)a[4] << 20) | ((long)a[5] << 25);
+#else
   if ((*a | PIT_COMBINE) & ~31) 
     return HASHOVERFLOW;
   a -= PITS-1;
@@ -70,2 +76,3 @@ static inline long packpits(char *a) {
   ;
+#endif
   }
diff --git a/rules.c b/rules.c
--- a/rules.c
+++ b/rules.c
@@ -41,7 +41,7 @@ int move(position *ps, int i) {
     s = p - sp;
     ep--;
-    if (s>PITS && *p==1 && s<LPIT) {
+    if (s>PITS && *p==1 && s<LPIT && sp[LPIT-1-s]) {
       *ep += sp[LPIT-1-s];
       (*ep)++;
       sp[LPIT-1-s] = 0;
       *p = 0;
@@ -63,7 +63,7 @@ int move(position *ps, int i) {
     s = p - sp;
     ep = sp + PITS;
-    if (s<PITS && *p==1) {
+    if (s<PITS && *p==1 && sp[LPIT-1-s]) {
       *ep += sp[LPIT-1-s];
       (*ep)++;
       sp[LPIT-1-s] = 0;
       *p = 0;
@@ -90,5 +90,5 @@ int move(position *ps, int i) {
   p = sp;
-  if (!(*p || EMPTY_PIT_CHECK)) { 
+  if ((i ? ps->s : 1-ps->s) == 0 && !(*p || EMPTY_PIT_CHECK)) {
     ep = sp + LPIT;
     i = *ep;
     p = sp + PITS + 1;
@@ -105,5 +105,5 @@ int move(position *ps, int i) {
   p = sp + PITS+1;
-  if (!(*p || EMPTY_PIT_CHECK)) { 
+  if ((i ? ps->s : 1-ps->s) == 1 && !(*p || EMPTY_PIT_CHECK)) {
     ep = sp + PITS;
     p = sp;
     i = *ep;
diff --git a/hash.cilk b/hash.cilk
--- a/hash.cilk
+++ b/hash.cilk
@@ -78,2 +78,7 @@ extern void setstale() {
 void ha_lookup(datumaccess *da, position *p) {
+#ifdef DISABLE_TT
+  da->ko = 1;
+  da->t = DV_INVALID;
+  return;
+#endif
   ub4 k;
@@ -129,2 +134,5 @@ void ha_lookup(datumaccess *da, position *p) {
 void da_seta(datumaccess *da, int d, int m, int r, int t) {
+#ifdef DISABLE_TT
+  return;
+#endif
   if (da->ko) return;
diff --git a/crunch.cilk b/crunch.cilk
--- a/crunch.cilk
+++ b/crunch.cilk
@@ -99,1 +99,2 @@ static int peek(c_res *s, int d, int a) {
+#ifndef DISABLE_FUTILITY
   /* futility pruning */
@@ -111,1 +112,2 @@ static int peek(c_res *s, int d, int a) {
+#endif
   /* transposition table */ 
diff --git a/endgame.h b/endgame.h
--- a/endgame.h
+++ b/endgame.h
@@ -90,7 +90,7 @@ extern inline int eg_getd(size_t i) {
 extern inline int eg_lookup(int t, position *p, int *q) { 
   int r = eg_getd(eg_index(t,p));
   *q = (r == 15 && eg.bits < 5) ? EG_LO : EG_EXACT; 
-  return (++r << 1) - t + rate(*p);
+  return (r << 1) - t + rate(*p);
   }
 
 extern inline void eg_setd_4_explicit(struct endgame *e, size_t i, int v) {
diff --git a/generator.c b/generator.c
--- a/generator.c
+++ b/generator.c
@@ -113,5 +113,5 @@ int crunch(int n, size_t l, position *p) {
   int i,s,v,r,e,cn;
   size_t k;
   position t;
-  v = 1;
+  v = 0;
   p->a[LPIT] = p->a[PITS] = 0;
@@ -130,11 +130,11 @@ int crunch(int n, size_t l, position *p) {
           r = e ? r + s : n - s;
           }
         else
-          r = e ? r + s + 1 : n - s - 1;
+          r = e ? r + s : n - s;
         }
       if (v < r)
         v = r;
       }
-  eg_setd(l,v-1);
+  eg_setd(l,v);
   crunched++;
   return v;
