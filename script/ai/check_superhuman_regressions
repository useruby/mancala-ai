#!/usr/bin/env python3
from __future__ import annotations

import argparse
import json
import os
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(REPO_ROOT))

DEFAULT_POSITIONS_PATH = Path("test/fixtures/ai/superhuman_regression_positions.json")
DEFAULT_ARTIFACT_PATH = Path("model-artifact/current")
DEFAULT_SEED = 17
DEFAULT_C_PUCT = 1.25


def resolve_input_path(path_value: str) -> str:
    path = Path(path_value)
    if path.is_absolute() or path.exists():
        return str(path)

    repo_path = REPO_ROOT / path
    if repo_path.exists():
        return str(repo_path)

    return str(path)


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(
        prog="script/ai/check_superhuman_regressions",
        description="Evaluate AlphaZero-lite regression positions against an artifact.",
    )
    parser.add_argument("--positions", default=str(DEFAULT_POSITIONS_PATH))
    parser.add_argument("--artifact", default=str(DEFAULT_ARTIFACT_PATH))
    parser.add_argument("--simulations", type=int)
    parser.add_argument("--out")
    parser.add_argument("--fpu-mode")
    parser.add_argument(
        "--reuse-subtree",
        dest="reuse_subtree",
        action=argparse.BooleanOptionalAction,
        default=None,
    )
    parser.add_argument(
        "--normalize-values",
        dest="normalize_values",
        action=argparse.BooleanOptionalAction,
        default=None,
    )
    parser.add_argument("--root-policy-mode")
    parser.add_argument("--tactical-root-bias", type=float)
    return parser


def stub_payload(args: argparse.Namespace) -> dict:
    return {
        "passed": True,
        "artifact_path": args.artifact,
        "positions_path": args.positions,
        "results": [],
    }


def search_option_overrides(args: argparse.Namespace) -> dict:
    overrides = {}
    if args.fpu_mode is not None:
        overrides["fpu_mode"] = args.fpu_mode
    if args.reuse_subtree is not None:
        overrides["reuse_subtree"] = bool(args.reuse_subtree)
    if args.normalize_values is not None:
        overrides["normalize_values"] = bool(args.normalize_values)
    if args.root_policy_mode is not None:
        overrides["root_policy_mode"] = args.root_policy_mode
    if args.tactical_root_bias is not None:
        overrides["tactical_root_bias"] = float(args.tactical_root_bias)
    return overrides


def write_payload(payload: dict, out_path: str | None) -> None:
    rendered = json.dumps(payload, indent=2)
    if out_path:
        Path(out_path).write_text(rendered + "\n", encoding="utf-8")
    print(rendered)


def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)

    if os.environ.get("AZLITE_CHECK_SUPERHUMAN_REGRESSIONS_STUB") == "1":
        write_payload(stub_payload(args), args.out)
        return 0

    from ml.alphazero_lite import superhuman_regressions

    overrides = search_option_overrides(args)
    positions_path = resolve_input_path(args.positions)
    positions = superhuman_regressions.load_regression_positions(positions_path)
    results = superhuman_regressions.evaluate_regression_positions(
        positions=positions,
        artifact_path=args.artifact,
        simulations=args.simulations,
        seed=DEFAULT_SEED,
        c_puct=DEFAULT_C_PUCT,
        search_options=(
            superhuman_regressions.build_search_options(**overrides)
            if overrides
            else None
        ),
    )
    payload = superhuman_regressions.build_regression_report(
        artifact_path=args.artifact,
        positions_path=positions_path,
        results=results,
    )
    write_payload(payload, args.out)
    return 0 if payload["passed"] else 1


if __name__ == "__main__":
    raise SystemExit(main())
